
spring:
  application:
    name: bee-ui
  profiles:
    active: #spring.profiles.active#
  main:
    allow-bean-definition-overriding: true

vaadin:
  compatibilityMode: false

server:
  port: 9070

